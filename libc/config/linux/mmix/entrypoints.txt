# Only entries with buildable dependencies belong in the initial archive.
set(TARGET_LIBC_ENTRYPOINTS
    libc.src.errno.errno
    libc.src.string.memcpy
    libc.src.string.memmove
    libc.src.string.memset
    libc.src.string.memcmp
    libc.src.string.strlen
    libc.src.unistd.getpid
    libc.src.unistd._exit
    libc.src.stdlib._Exit
)

# read/write/close/lseek remain compile-only until their full closure is qualified.

set(TARGET_LLVMLIBC_ENTRYPOINTS ${TARGET_LIBC_ENTRYPOINTS})

include(${CMAKE_CURRENT_LIST_DIR}/RuntimeState.cmake)
